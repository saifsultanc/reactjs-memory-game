import React from "react";
const Winner = () => {
  return (
    <section className="modal-overlay">
      <section className="modal">
        <h2 className="winner">You Win!</h2>
      </section>
    </section>
  );
};

export default Winner;
